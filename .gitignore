
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Session / auth state (contains sensitive tokens — do not commit)
user-session.json
